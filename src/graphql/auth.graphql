# Mutations
mutation GenerateTempToken($email: String!) {
  generateTempToken(email: $email) {
    token
  }
}

mutation SendOTP($email: String!) {
  sendOTP(email: $email) {
    success
    message
  }
}
