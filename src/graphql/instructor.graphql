# Query
query GetInstructorCourseContent($slug: String!) {
  getInstructorCourseContent(slug: $slug) {
    _id
    sectionId {
      _id
      title
      description
      lessonId {
        _id
        title
      }
    }
  }
}
